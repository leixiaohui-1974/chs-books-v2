
Opening authentication page in your browser. Do you want to continue? [Y/n]: 

---

## 仿真代码解读

> 本节由Codex引擎生成，提供本章核心算法的Python实现与解读。

```python
# -*- coding: utf-8 -*-
"""
教材：《人工智能与水利水电工程》
章节：第3章 大语言模型（LLM）原理 - 3.1 基本概念与理论框架
功能：构建“词元嵌入 -> 自注意力 -> 概率预测”的最小仿真，打印KPI结果表并生成可视化图。
依赖：numpy / scipy / matplotlib
"""

import numpy as np
from scipy.special import softmax
from scipy.optimize import minimize
import matplotlib.pyplot as plt


# =========================
# 1) 关键参数（集中定义，便于教学调参）
# =========================
RANDOM_SEED = 42
VOCAB_SIZE = 18          # 词表规模
SEQ_LEN = 8              # 上下文长度
EMBED_DIM = 24           # 词向量维度
TRAIN_SAMPLES = 1200     # 训练样本数
TEST_SAMPLES = 300       # 测试样本数
MAX_ITER = 140           # 优化最大迭代次数
L2_REG = 1e-3            # L2正则
ATTN_TEMP = 1.0          # 注意力温度（越小越“尖锐”）
TOPK = 3                 # Top-k准确率
SAVE_FIG = True
FIG_PATH = "llm_ch3_1_simulation.png"

np.random.seed(RANDOM_SEED)


# =========================
# 2) 构造“水利工程语境”词元
# =========================
vocab = [
    "降雨", "径流", "蒸发", "水位", "流量", "闸门", "泵站", "渠道",
    "水库", "洪峰", "预报", "调度", "传感器", "泥沙", "渗流", "坝体",
    "告警", "优化"
]


# =========================
# 3) 构造语料统计规律（马尔可夫转移）
# =========================
base = np.ones((VOCAB_SIZE, VOCAB_SIZE)) * 0.2
for i in range(VOCAB_SIZE):
    base[i, (i + 1) % VOCAB_SIZE] += 2.0
    base[i, (i + 2) % VOCAB_SIZE] += 1.2

# 强化一些与水利业务相关的语义链路
def boost(a, b, w):
    ia, ib = vocab.index(a), vocab.index(b)
    base[ia, ib] += w

boost("降雨", "径流", 4.0)
boost("径流", "洪峰", 3.5)
boost("洪峰", "告警", 3.0)
boost("传感器", "预报", 3.5)
boost("预报", "调度", 3.0)
boost("调度", "闸门", 3.2)
boost("闸门", "水位", 2.8)
boost("水库", "调度", 2.5)
boost("坝体", "渗流", 2.8)

P = base / base.sum(axis=1, keepdims=True)


def sample_sequence(length):
    """按转移矩阵采样一个词元序列"""
    seq = np.zeros(length, dtype=np.int64)
    seq[0] = np.random.randint(0, VOCAB_SIZE)
    for t in range(1, length):
        seq[t] = np.random.choice(VOCAB_SIZE, p=P[seq[t - 1]])
    return seq


def build_dataset(n_samples):
    """构造监督学习数据：X为上下文，y为下一词"""
    X = np.zeros((n_samples, SEQ_LEN), dtype=np.int64)
    y = np.zeros(n_samples, dtype=np.int64)
    for i in range(n_samples):
        seq = sample_sequence(SEQ_LEN + 1)
        X[i] = seq[:SEQ_LEN]
        y[i] = seq[SEQ_LEN]
    return X, y


X_train, y_train = build_dataset(TRAIN_SAMPLES)
X_test, y_test = build_dataset(TEST_SAMPLES)


# =========================
# 4) 词嵌入 + 位置嵌入（固定表示，模拟预训练）
# =========================
E = np.random.randn(VOCAB_SIZE, EMBED_DIM) * 0.5       # 词嵌入
Pos = np.random.randn(SEQ_LEN, EMBED_DIM) * 0.1        # 位置嵌入


def attention_features(X, temp=1.0):
    """
    计算注意力上下文向量：
    输入 X: [N, L]
    输出 C: [N, D], alpha: [N, L]
    """
    H = E[X] + Pos[None, :, :]          # [N, L, D]
    q = H[:, -1, :]                     # 使用最后位置作为查询向量
    scores = np.einsum("nld,nd->nl", H, q) / np.sqrt(EMBED_DIM)
    scores = scores / max(temp, 1e-8)
    alpha = softmax(scores, axis=1)     # 注意力分布
    C = np.einsum("nl,nld->nd", alpha, H)
    return C, alpha


C_train, alpha_train = attention_features(X_train, ATTN_TEMP)
C_test, alpha_test = attention_features(X_test, ATTN_TEMP)


# =========================
# 5) 训练输出层（SciPy优化）
# logits = C @ W + b
# =========================
D, K = EMBED_DIM, VOCAB_SIZE

def unpack(theta):
    W = theta[:D * K].reshape(D, K)
    b = theta[D * K:]
    return W, b


def nll_and_grad(theta, Xf, y, reg):
    W, b = unpack(theta)
    logits = Xf @ W + b
    probs = softmax(logits, axis=1)

    n = Xf.shape[0]
    loss = -np.log(probs[np.arange(n), y] + 1e-12).mean()
    loss += 0.5 * reg * np.sum(W * W)

    G = probs.copy()
    G[np.arange(n), y] -= 1.0
    G /= n
    gW = Xf.T @ G + reg * W
    gb = G.sum(axis=0)
    grad = np.concatenate([gW.ravel(), gb])
    return loss, grad


init = np.random.randn(D * K + K) * 0.01
loss_trace = []

def callback(theta):
    l, _ = nll_and_grad(theta, C_train, y_train, L2_REG)
    loss_trace.append(l)

res = minimize(
    fun=lambda th: nll_and_grad(th, C_train, y_train, L2_REG),
    x0=init,
    jac=True,
    method="L-BFGS-B",
    callback=callback,
    options={"maxiter": MAX_ITER, "disp": False}
)

W_opt, b_opt = unpack(res.x)


# =========================
# 6) 评估指标（KPI）
# =========================
def evaluate_attention(C, y, W, b, topk=3):
    logits = C @ W + b
    probs = softmax(logits, axis=1)
    n = len(y)
    ce = -np.log(probs[np.arange(n), y] + 1e-12).mean()
    ppl = np.exp(ce)
    pred1 = np.argmax(probs, axis=1)
    acc1 = (pred1 == y).mean()
    topk_idx = np.argpartition(-probs, kth=topk - 1, axis=1)[:, :topk]
    acck = np.mean([y[i] in topk_idx[i] for i in range(n)])
    return ce, ppl, acc1, acck, probs


# Bigram基线：仅看最后一个词预测下一词
counts = np.ones((VOCAB_SIZE, VOCAB_SIZE)) * 0.1
for x, yt in zip(X_train, y_train):
    counts[x[-1], yt] += 1.0
P_bigram = counts / counts.sum(axis=1, keepdims=True)

def evaluate_bigram(X, y, Pbg, topk=3):
    probs = Pbg[X[:, -1]]
    n = len(y)
    ce = -np.log(probs[np.arange(n), y] + 1e-12).mean()
    ppl = np.exp(ce)
    pred1 = np.argmax(probs, axis=1)
    acc1 = (pred1 == y).mean()
    topk_idx = np.argpartition(-probs, kth=topk - 1, axis=1)[:, :topk]
    acck = np.mean([y[i] in topk_idx[i] for i in range(n)])
    return ce, ppl, acc1, acck, probs


ce_b, ppl_b, acc1_b, acck_b, probs_b = evaluate_bigram(X_test, y_test, P_bigram, TOPK)
ce_t, ppl_t, acc1_t, acck_t, probs_t = evaluate_attention(C_test, y_test, W_opt, b_opt, TOPK)


# =========================
# 7) 打印KPI表格
# =========================
print("\n=== KPI结果表（测试集）===")
print(f"{'模型':<20}{'CrossEntropy':>14}{'Perplexity':>12}{'Top1Acc':>10}{'Top3Acc':>10}")
print("-" * 66)
print(f"{'Bigram基线':<20}{ce_b:>14.4f}{ppl_b:>12.4f}{acc1_b:>10.4f}{acck_b:>10.4f}")
print(f"{'注意力+线性头':<20}{ce_t:>14.4f}{ppl_t:>12.4f}{acc1_t:>10.4f}{acck_t:>10.4f}")


# =========================
# 8) 生成matplotlib图
# =========================
idx = 0
x_demo = X_test[idx]
y_demo = y_test[idx]
_, alpha_demo = attention_features(X_test[idx:idx + 1], ATTN_TEMP)
alpha_demo = alpha_demo[0]
demo_probs = probs_t[idx]

topn = 8
top_idx = np.argsort(-demo_probs)[:topn]

fig, axes = plt.subplots(1, 3, figsize=(16, 4.8))

# 图1：训练损失下降
if len(loss_trace) > 0:
    axes[0].plot(np.arange(1, len(loss_trace) + 1), loss_trace, color="#2a9d8f", lw=2)
axes[0].set_title("优化过程：训练损失")
axes[0].set_xlabel("迭代步")
axes[0].set_ylabel("NLL Loss")

# 图2：注意力权重
axes[1].bar(np.arange(SEQ_LEN), alpha_demo, color="#1f77b4", alpha=0.85)
axes[1].set_xticks(np.arange(SEQ_LEN))
axes[1].set_xticklabels([vocab[t] for t in x_demo], rotation=30)
axes[1].set_title("自注意力权重")
axes[1].set_xlabel("上下文词元位置")
axes[1].set_ylabel("权重")

# 图3：下一词Top概率
axes[2].bar(np.arange(topn), demo_probs[top_idx], color="#ff7f0e", alpha=0.85)
axes[2].set_xticks(np.arange(topn))
axes[2].set_xticklabels([vocab[t] for t in top_idx], rotation=30)
axes[2].set_title("下一词预测Top概率")
axes[2].set_xlabel("候选词元")
axes[2].set_ylabel("概率")

plt.tight_layout()
if SAVE_FIG:
    plt.savefig(FIG_PATH, dpi=150)
    print(f"\n图已保存：{FIG_PATH}")
plt.show()

print("\n示例上下文：", " | ".join(vocab[t] for t in x_demo))
print("真实下一词：", vocab[y_demo])
print("模型预测：", vocab[np.argmax(demo_probs)])
```

代码解读（约800字）  
这段脚本把LLM在3.1里最核心的理论链路压缩成一个可教学、可运行、可观察的最小系统。第一步是“离散符号到连续表示”：脚本先定义水利语境词表，再通过`E`把词元索引映射到向量空间，这对应LLM的嵌入层思想。为了让模型区分顺序，加入了`Pos`位置嵌入，体现“同一词在不同位置语义作用不同”的框架。第二步是“上下文建模”：`attention_features`里用最后位置向量做Query，与全序列做缩放点积，经过softmax得到注意力分布`alpha`。这里的`alpha`可直接解释为“模型在预测下一词时看重哪些历史词元”，这就是注意力机制可解释性的教学价值。第三步是“概率建模与训练目标”：上下文向量`C`经线性头`W,b`输出logits，再softmax成条件概率`P(next_token | context)`，损失函数采用交叉熵，完全对应语言模型的最大似然训练思路。第四步是“优化算法”：没有用深度学习框架，而是用`scipy.optimize.minimize`的L-BFGS-B做参数更新，便于学生看到“目标函数+梯度+迭代”这一通用数值优化范式。第五步是“KPI评估体系”：脚本输出CrossEntropy、Perplexity、Top1Acc、Top3Acc。交叉熵越低说明分布拟合越好；困惑度是交叉熵指数化后更直观的“不确定度”；Top1/Top3分别反映严格命中与候选覆盖能力，适合工程场景下的智能决策辅助。第六步是“基线对照实验”：Bigram只看最后一个词，注意力模型看完整上下文，二者同场比较可让学生理解“长上下文依赖”为什么是LLM能力提升关键。第七步是“可视化验证”：损失曲线展示训练收敛，注意力柱状图展示模型关注焦点，Top概率图展示输出分布结构，三张图分别对应训练过程、机理解释、推理结果。参数层面，`SEQ_LEN`控制记忆窗口，`EMBED_DIM`控制表示容量，`ATTN_TEMP`控制注意力尖锐程度，`L2_REG`控制泛化稳健性；这些参数都被集中定义，便于课堂上做灵敏度实验。整体上，该脚本不是工业级大模型，而是一个面向教材的“理论到代码”映射器：从词元化、嵌入、注意力、概率输出、损失优化到KPI评估形成闭环，恰好覆盖3.1“基本概念与理论框架”的主干内容。


## 参考文献

1. Goodfellow, I., Bengio, Y., & Courville, A. (2016). *Deep Learning*. MIT Press.
2. Shen, C. (2018). A Transdisciplinary Review of Deep Learning Research and Its Relevance for Water Resources Scientists. *Water Resources Research*, 54(11), 8558-8593.
3. Kratzert, F., et al. (2018). Rainfall–runoff modelling using Long Short-Term Memory (LSTM) networks. *Hydrology and Earth System Sciences*, 22(11), 6005-6022.
4. Lei et al. (2025a). 水系统控制论：基本原理与理论框架. *南水北调与水利科技(中英文)*. DOI: 10.13476/j.cnki.nsbdqk.2025.0077
5. Kratzert, F., et al. (2019). Towards learning universal, regional, and local hydrological behaviors via machine learning applied to large-sample datasets. *Hydrology and Earth System Sciences*, 23(12), 5089-5110.

---

## 本章小结

本章围绕大语言模型（LLM）的核心原理，从词元表示、注意力机制、训练目标到水利工程应用前景，构建了理解现代生成式AI的完整理论框架。

1. **词元化与分布式表示**：大语言模型的输入处理始于将文本切分为离散词元（Token），再通过可学习的嵌入矩阵 $\mathbf{E} \in \mathbb{R}^{|\mathcal{V}|\times d}$ 将每个词元映射为连续向量空间中的稠密表示。这种分布式表示使语义相近的词元在嵌入空间中距离较近，具备代数运算特性（如"水库" - "调度" + "预报" $\approx$ "洪水"）。位置编码 $\mathbf{PE}$ 叠加在嵌入向量上，使模型感知序列中词元的相对或绝对位置，是处理有序文本不可缺少的结构要素。

2. **缩放点积注意力机制**：自注意力（Self-Attention）的核心运算为 $\text{Attention}(\mathbf{Q},\mathbf{K},\mathbf{V}) = \text{softmax}\left(\frac{\mathbf{Q}\mathbf{K}^\top}{\sqrt{d_k}}\right)\mathbf{V}$，其中查询矩阵 $\mathbf{Q}$、键矩阵 $\mathbf{K}$、值矩阵 $\mathbf{V}$ 均由输入序列线性变换得到。$\sqrt{d_k}$ 的缩放因子防止向量维度增大时点积进入 softmax 的饱和区，保证梯度流畅传播。多头注意力（Multi-Head Attention）在多个子空间并行执行上述运算，使模型能同时捕获语义、句法、指代等不同层次的依赖关系。

3. **Transformer架构与训练目标**：完整的Transformer编码器由"多头注意力 + 前馈网络 + 残差连接 + 层归一化"的标准模块堆叠构成，残差连接 $\mathbf{x}_{l+1} = \mathbf{x}_l + \mathcal{F}(\mathbf{x}_l)$ 缓解了深层网络的梯度消失问题。语言模型采用自回归训练范式，以最大化条件对数似然 $\sum_t \log P(w_t | w_1, \ldots, w_{t-1}; \boldsymbol{\theta})$ 为目标，等价于最小化交叉熵损失。困惑度 $\text{PPL} = \exp\bigl(\frac{1}{T}\sum_t \mathcal{L}_t\bigr)$ 是评估语言模型预测质量的标准度量，越低表明模型对真实文本分布拟合越好。

4. **LLM与简单统计模型的本质差异**：Bigram 基线仅利用前一词元预测下一词元，本质上是一阶马尔可夫假设，无法捕获长距离语义依赖。自注意力模型通过全局感受野使每个词元的表示都能整合任意位置的上下文信息，突破了马尔可夫链的局部性限制。实验对比表明，随着上下文长度和模型参数规模的增加，注意力模型相对Bigram基线的优势持续扩大，这正是"涌现能力"产生的基础。

5. **LLM在水利工程中的应用前景与挑战**：LLM凭借强大的自然语言理解与生成能力，在水利工程领域展现出多维度应用潜力，包括洪水预报文本报告自动生成、水文知识问答系统、历史水文资料的非结构化信息抽取以及调度方案的自然语言交互界面。然而，LLM在数值推理精度（如精确的水力计算）、物理约束遵守（如质量守恒）和幻觉（Hallucination）抑制方面仍存在根本性挑战，将LLM与物理驱动模型深度融合（Physics-Informed LLM）是当前研究前沿。

---

## 思考与练习

**1.** 词元嵌入（Token Embedding）和位置编码（Positional Encoding）在Transformer中分别承担什么功能？请解释为何不将位置信息直接编码进词元嵌入，而是单独叠加位置向量。比较正弦-余弦固定位置编码与可学习位置编码在外推能力（处理比训练序列更长的输入）方面的本质差异。

**2.** 缩放点积注意力公式为 $\text{Attention}(\mathbf{Q},\mathbf{K},\mathbf{V}) = \text{softmax}(\mathbf{Q}\mathbf{K}^\top/\sqrt{d_k})\mathbf{V}$。请解释：①为何要除以 $\sqrt{d_k}$ 而非其他因子；②在自回归语言模型（如GPT系列）中，为防止当前位置"看到"未来词元，需要对注意力权重矩阵施加因果掩码（Causal Mask），请推导掩码的具体实现方式；③多头注意力相对于单头注意力的理论优势是什么。

**3.** 语言模型的困惑度（Perplexity）被定义为 $\text{PPL} = \exp\bigl(-\frac{1}{T}\sum_{t=1}^T \log P(w_t|w_{<t})\bigr)$。假设一个Bigram模型在包含200个水文专业词汇的语料上训练，测试集困惑度为85；而同语料训练的注意力模型困惑度为12。请从信息论角度解释困惑度的物理意义（等效分支数），并分析在水利工程文本（富含领域术语和数值）的场景下，相比通用文本语料，预训练LLM可能面临哪些特殊的泛化挑战。

**4.** 设计一个面向水库调度辅助决策的LLM应用系统架构。需说明：①如何利用检索增强生成（RAG）技术将实时水文监测数据和历史调度规则与LLM的文本生成能力结合；②如何通过提示工程（Prompt Engineering）引导模型输出符合物理约束的调度建议；③如何设计评估指标体系衡量LLM辅助决策系统的可靠性，特别是针对幻觉输出的检测方法。

**5.** 近年来，将大语言模型与物理模型结合的"物理信息增强LLM"（Physics-Informed LLM）成为研究热点。请调研2020年以来至少3篇相关代表性文献，综述在水文水资源领域将LLM与数值模拟模型耦合的主要技术路线（如LLM作为代理模型、LLM辅助参数率定、LLM驱动多智能体优化等），分析各路线的适用场景、技术瓶颈，并展望未来5年该方向最可能取得突破的子问题。

