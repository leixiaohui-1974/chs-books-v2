
Opening authentication page in your browser. Do you want to continue? [Y/n]: 

---

## 仿真代码解读

> 本节由Codex引擎生成，提供本章核心算法的Python实现与解读。

本任务未调用现有 skill（`skill-creator/installer/slides/spreadsheets`），因为需求是数值仿真建模脚本编写，与这些技能描述不直接匹配。

```python
# -*- coding: utf-8 -*-
# 教材：《水-能源-粮食纽带关系》
# 章节：第3章 水-粮耦合建模（3.1 基本概念与理论框架）
# 功能：构建“水资源分配-粮食生产-需求反馈”耦合仿真，输出KPI并绘图

import numpy as np
from scipy.optimize import minimize_scalar
import matplotlib.pyplot as plt

# =========================
# 1) 关键参数（可调）
# =========================
YEARS = 30
START_YEAR = 2025
np.random.seed(42)

# 人口与粮食需求参数
pop0 = 5.0e6                 # 初始人口（人）
pop_growth = 0.008           # 人口年增长率
food_per_capita = 420.0      # 人均粮食需求（kg/人/年）
food_loss_rate = 0.05        # 粮食损耗率

# 水资源参数
inflow_mean = 1.25e9         # 年均来水（m3/年）
inflow_amp = 0.22            # 周期波动幅度
inflow_noise = 0.10          # 随机扰动强度
eco_min = 2.1e8              # 生态需水底线（m3/年）
domestic_per_capita = 48.0   # 居民生活用水（m3/人/年）

# 农业生产参数
area0 = 3.0e5                # 初始耕地面积（ha）
area_min, area_max = 2.4e5, 3.6e5
area_adjust_speed = 0.03     # 耕地调整速度（受缺粮反馈影响）
yield_max = 9200.0           # 单位面积理论最高产量（kg/ha）
water_prod_coeff = 0.0012    # 水分生产函数系数（1/(m3/ha)）
tech_trend = 0.004           # 年技术进步率

# 分水决策与惩罚参数
alpha_base = 0.72            # 农业分水基础比例
feedback_gain = 0.18         # 缺粮反馈增益
stress_penalty = 0.35        # 高水压力惩罚系数

# =========================
# 2) 状态变量初始化
# =========================
years = np.arange(START_YEAR, START_YEAR + YEARS)

pop = np.zeros(YEARS)
inflow = np.zeros(YEARS)
area = np.zeros(YEARS)
irri_water = np.zeros(YEARS)
domestic_water = np.zeros(YEARS)
water_stress = np.zeros(YEARS)

production = np.zeros(YEARS)
demand = np.zeros(YEARS)
food_gap = np.zeros(YEARS)
ssr = np.zeros(YEARS)       # 自给率
wue = np.zeros(YEARS)       # 水分利用效率（kg/m3）
alpha_opt = np.zeros(YEARS) # 优化得到的农业分水比例

pop[0] = pop0
area[0] = area0

# =========================
# 3) 年度耦合仿真
# =========================
for t in range(YEARS):
    if t > 0:
        pop[t] = pop[t - 1] * (1 + pop_growth)

        # 根据上一年缺粮率调整耕地（反馈机制）
        prev_gap_ratio = max(0.0, food_gap[t - 1] / max(demand[t - 1], 1.0))
        area[t] = np.clip(
            area[t - 1] * (1 + area_adjust_speed * (prev_gap_ratio - 0.02)),
            area_min,
            area_max,
        )

    # 来水 = 周期项 + 随机项
    seasonal = 1 + inflow_amp * np.sin(2 * np.pi * t / 7)
    random_term = 1 + inflow_noise * np.random.randn()
    inflow[t] = max(0.55 * inflow_mean, inflow_mean * seasonal * random_term)

    domestic_water[t] = pop[t] * domestic_per_capita
    available_for_alloc = max(0.0, inflow[t] - eco_min - domestic_water[t])

    # 粮食需求
    demand[t] = pop[t] * food_per_capita

    # 缺粮反馈到分水上限：缺粮越大，农业分水上限越高
    if t == 0:
        alpha_cap = min(0.95, alpha_base + 0.05)
    else:
        gap_ratio = max(0.0, food_gap[t - 1] / max(demand[t - 1], 1.0))
        alpha_cap = np.clip(alpha_base + feedback_gain * gap_ratio, 0.35, 0.95)

    # 使用 scipy 优化农业分水比例 alpha
    def objective(alpha):
        agri_w = alpha * available_for_alloc
        water_per_ha = agri_w / max(area[t], 1.0)

        # 灌溉-产量边际递减函数
        tech_factor = (1 + tech_trend) ** t
        y_ha = yield_max * (1 - np.exp(-water_prod_coeff * water_per_ha)) * tech_factor
        q = y_ha * area[t]

        # 目标：缺粮最小 + 高水压力惩罚
        shortage = max(0.0, demand[t] - q)
        total_withdrawal = eco_min + domestic_water[t] + agri_w
        stress = total_withdrawal / max(inflow[t], 1.0)

        return shortage / 1e6 + stress_penalty * max(0.0, stress - 0.85) ** 2

    res = minimize_scalar(objective, bounds=(0.1, alpha_cap), method="bounded")
    alpha_star = float(res.x)
    alpha_opt[t] = alpha_star

    # 计算本年结果
    irri_water[t] = alpha_star * available_for_alloc
    water_per_ha = irri_water[t] / max(area[t], 1.0)

    tech_factor = (1 + tech_trend) ** t
    y_ha = yield_max * (1 - np.exp(-water_prod_coeff * water_per_ha)) * tech_factor
    production[t] = y_ha * area[t] * (1 - food_loss_rate)

    food_gap[t] = demand[t] - production[t]
    ssr[t] = production[t] / demand[t] if demand[t] > 0 else np.nan
    wue[t] = production[t] / max(irri_water[t], 1.0)

    total_withdrawal = eco_min + domestic_water[t] + irri_water[t]
    water_stress[t] = total_withdrawal / max(inflow[t], 1.0)

# =========================
# 4) KPI结果表格
# =========================
kpi = {
    "平均粮食自给率": np.mean(ssr),
    "粮食供给可靠度(SSR>=1)": np.mean(ssr >= 1.0),
    "平均水压力指数": np.mean(water_stress),
    "高水压年份占比(>0.9)": np.mean(water_stress > 0.9),
    "平均水分利用效率(kg/m3)": np.mean(wue),
    "年均缺粮量(万吨)": np.mean(np.maximum(food_gap, 0.0)) / 1e7,
}

print("\n" + "=" * 64)
print("水-粮耦合模型 KPI 结果")
print("=" * 64)
print(f"{'指标':<34}{'数值':>20}")
print("-" * 64)
for name, value in kpi.items():
    if ("占比" in name) or ("可靠度" in name) or ("自给率" in name):
        print(f"{name:<34}{value:>19.2%}")
    elif "水压力指数" in name:
        print(f"{name:<34}{value:>20.3f}")
    else:
        print(f"{name:<34}{value:>20.3f}")
print("=" * 64)

# =========================
# 5) matplotlib 绘图
# =========================
fig, axes = plt.subplots(2, 2, figsize=(12, 8), dpi=120)

axes[0, 0].plot(years, inflow / 1e8, label="可利用来水")
axes[0, 0].plot(years, irri_water / 1e8, label="农业灌溉用水")
axes[0, 0].set_title("水资源供给与农业取水")
axes[0, 0].set_ylabel("亿 m3")
axes[0, 0].legend()
axes[0, 0].grid(alpha=0.3)

axes[0, 1].plot(years, demand / 1e6, label="粮食需求")
axes[0, 1].plot(years, production / 1e6, label="粮食产量")
axes[0, 1].set_title("粮食供需演化")
axes[0, 1].set_ylabel("百万 kg")
axes[0, 1].legend()
axes[0, 1].grid(alpha=0.3)

axes[1, 0].plot(years, ssr, label="自给率 SSR")
axes[1, 0].axhline(1.0, color="r", ls="--", lw=1, label="供需平衡线")
axes[1, 0].set_title("粮食自给率")
axes[1, 0].set_ylim(0.6, max(1.2, np.max(ssr) + 0.05))
axes[1, 0].legend()
axes[1, 0].grid(alpha=0.3)

axes[1, 1].plot(years, water_stress, label="水压力指数")
axes[1, 1].plot(years, alpha_opt, label="农业分水比例 alpha")
axes[1, 1].axhline(0.9, color="r", ls="--", lw=1, label="高水压阈值")
axes[1, 1].set_title("耦合反馈：水压力与分水决策")
axes[1, 1].legend()
axes[1, 1].grid(alpha=0.3)

for ax in axes.flat:
    ax.set_xlabel("年份")

fig.suptitle("第3章 水-粮耦合建模仿真结果", fontsize=14)
fig.tight_layout()

plt.savefig("water_food_coupling_simulation.png", bbox_inches="tight")
plt.show()
```

代码解读（约800字）：

本脚本以“水-粮耦合”为核心，按照第3章3.1节的理论框架，把系统拆成三个层面：资源供给层、生产转化层、社会需求层。资源供给层用`inflow`表示年度可利用来水，并加入周期波动和随机扰动，模拟气候与水文不确定性；同时设置`eco_min`作为生态底线，体现“先生态后分配”的原则。社会需求层由人口增长驱动，`demand = population × 人均粮食需求`，展示需求侧刚性增长。生产转化层通过“灌溉水→单产→总产”的函数链实现，其中单产采用指数饱和形式`1-exp(-k·水量强度)`，体现边际收益递减，符合农业水分生产常识。

耦合关系体现在两个反馈回路。第一，粮食缺口反馈到下一期资源分配：若上一年缺粮，则农业分水上限`alpha_cap`提高，使系统向保供方向调整。第二，缺粮也影响耕地面积`area`的缓慢变化，表示政策和经营主体对安全信号的中长期响应。这样，模型不是单向“给水算粮”，而是“缺粮-调水-再生产”的动态闭环，更贴近纽带关系分析所强调的跨部门联动。

在求解方法上，脚本没有直接固定农业取水比例，而是每年用`scipy.optimize.minimize_scalar`寻找最优`alpha`。目标函数由两部分组成：一是缺粮量（保供目标），二是水压力超阈值惩罚（可持续目标）。这相当于把“粮食安全”和“水资源安全”统一到同一决策中，避免只追求高产导致过度取水。`stress_penalty`参数可以理解为政策偏好：值越大，系统越保守；值越小，系统越偏向短期增产。

KPI部分对应教学中“模型可解释、可评价”的要求。`平均粮食自给率`和`供给可靠度`衡量安全性，`平均水压力指数`与`高水压年份占比`衡量资源风险，`水分利用效率`反映产出效率，`年均缺粮量`给出直观缺口规模。把这些指标同时展示，能帮助学习者理解“单指标最优未必系统最优”的纽带思维。图形输出则从时间序列角度观察机制：第一幅看供水与农业取水匹配，第二幅看供需是否长期偏离，第三幅看自给率是否稳定在1附近，第四幅直接展示“压力-决策”的反馈联动。

从教学使用看，这个脚本的价值在于参数可调、结构清晰、可快速扩展。你可以把能源模块接入（如抽水能耗、电价约束），或加入地下水库容、贸易进口、极端干旱情景，形成更完整的WEF耦合实验。当前版本作为3.1节入门案例，已经覆盖“概念建模—反馈设计—优化决策—指标评价”的完整链条，适合课堂演示与学生二次开发。补充说明：我在当前受限环境无法直接替你落盘运行，但脚本语法与依赖调用已按可执行形式给出。


## 参考文献

1. Hoff, H. (2011). Understanding the Nexus. *Background Paper for the Bonn 2011 Conference: The Water, Energy and Food Security Nexus*. Stockholm Environment Institute.
2. Bazilian, M., et al. (2011). Considering the energy, water and food nexus: Towards an integrated modelling approach. *Energy Policy*, 39(12), 7896-7906.
3. Albrecht, T. R., et al. (2018). The Water-Energy-Food Nexus: A systematic review of methods for nexus assessment. *Environmental Research Letters*, 13(4), 043002.
4. Lei et al. (2025a). 水系统控制论：基本原理与理论框架. *南水北调与水利科技(中英文)*. DOI: 10.13476/j.cnki.nsbdqk.2025.0077

## 本章小结

本章以水-粮耦合建模为核心，通过构建"水资源分配-粮食生产-需求反馈"的动态耦合仿真系统，系统展示了水粮纽带关系的建模思路、优化决策方法和综合评价体系：

- **水粮耦合的多层结构**将系统分解为资源供给层（来水、生态底线）、生产转化层（水分生产函数）和社会需求层（人口增长、粮食刚需），三层之间通过状态变量（耕地面积、灌溉水量）和反馈信号（粮食缺口、水压力指数）紧密耦合，体现了纽带系统"跨部门联动"的本质特征。
- **水分生产函数**采用指数饱和形式，将灌溉水量与作物单产联系起来，揭示了农业用水边际收益递减的规律，为定量评估"增加灌溉用水能带来多少增产"提供了可计算的理论基础。
- **双向反馈机制**是模型区别于简单线性计算的关键：粮食缺口反馈驱动下一期农业分水比例上调，而高水压力则触发惩罚，形成"保供-节水"的动态平衡，使模型具备学习和自我调节能力。
- **多目标优化决策**通过每年求解"缺粮惩罚+水压力惩罚"的标量化目标函数，将粮食安全目标与水资源可持续目标统一在同一决策框架内，避免了"单目标最优导致其他系统崩溃"的纽带陷阱。
- **综合KPI评价体系**涵盖安全性（粮食自给率、供给可靠度）、资源风险（水压力指数、高水压年份占比）和效率（水分利用效率）三个维度，支持政策情景比较分析，为区域水粮综合管理决策提供量化依据。

## 习题

1. 本章水分生产函数采用指数饱和形式：$Y = Y_{max} \cdot [1 - \exp(-k \cdot W_{irri}/A)]$，其中 $Y_{max}$ 为理论最高单产，$k$ 为水分利用系数，$W_{irri}/A$ 为单位耕地灌溉水深。请解释该函数的"边际收益递减"特性，并分析当灌溉水量趋于无穷大时单产的极限值，说明为何在缺水地区不能简单地"多给水就能多产粮"。

2. 模型中设置了生态需水底线 $W_{eco,min}$，即每年必须保留一定水量维持河道生态功能，剩余水量才能分配给农业和生活。请从"水资源总量-生态刚需-生活刚需-农业可用量"的层次关系出发，推导出农业可用灌溉水量的计算式，并分析在极端干旱年份（来水量仅为均值的60%）时，农业用水被压缩的幅度及其对粮食自给率的影响。

3. 参数 `stress_penalty`（水压力惩罚系数）控制着优化决策中对过度用水的容忍度。请比较以下两种政策情景的10年粮食自给率和平均水压力指数：（a）$stress\_penalty = 0.10$（偏向保供，宽松用水）；（b）$stress\_penalty = 0.60$（偏向节水，严格限水）。结合黄河流域水资源超载问题，讨论在实际政策制定中如何平衡这一权衡。

4. 本章仿真中人口以固定增长率增长，粮食需求随之刚性增加。请分析在以下两种情景叠加时对系统的冲击：（a）人口增长率从0.8%升至1.5%；（b）气候变化导致年均来水减少15%。这两种压力同时作用时，系统的粮食自给率和水压力指数将如何演变？请定性描述并说明可能的政策应对措施（如节水灌溉技术推广、食物进口补偿等）。

5. 编程实践：在本章仿真脚本基础上，增加能源模块，将抽水灌溉能耗纳入模型（假设提水能耗为0.35 kWh/m³，电价为0.6元/kWh）。修改KPI表格，增加"年均灌溉能耗（万kWh）"和"年均灌溉电费（万元）"两项指标，并分析在农业用水量从5亿m³增至8亿m³过程中，能源成本的增长是否与用水量成线性关系，从而初步揭示水-能耦合关系。
