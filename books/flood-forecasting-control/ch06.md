

---

## 仿真代码解读

> 本节由Codex引擎生成，提供本章核心算法的Python实现与解读。

```python
# -*- coding: utf-8 -*-
"""
教材：《洪水预报与防洪调度》
章节：第6章 洪水风险图与预警系统（6.1 基本概念与理论框架）
功能：基于“致灾因子-暴露度-脆弱性”框架构建洪水风险图，并评估分级预警KPI。
"""

import numpy as np
import matplotlib.pyplot as plt
from scipy import ndimage
from scipy.stats import genextreme, gamma

# =========================
# 1) 关键参数（可按教学需要调整）
# =========================
SEED = 42
N_EVENTS = 300                  # 蒙特卡洛洪水场次数
NX, NY = 140, 100               # 网格列数、行数
CELL_SIZE_M = 200               # 网格边长（米）
CELL_AREA_KM2 = (CELL_SIZE_M ** 2) / 1e6

# 洪峰流量GEV分布参数（用于随机生成洪水事件）
GEV_C = -0.12
GEV_LOC = 1800.0
GEV_SCALE = 420.0

# 水位-流量关系参数：H = H0 + A * Q^B
H0 = 24.0
RATING_A = 0.018
RATING_B = 0.62

# 河道外水位衰减（每1个网格距离衰减的水位，单位m）
DIST_ATTENUATION = 0.012

# 预报误差（对洪峰流量叠加高斯误差）
FORECAST_Q_STD = 220.0

# 预警阈值（单位：m）
LEVEL_YELLOW = 30.0
LEVEL_ORANGE = 31.0
LEVEL_RED = 32.0
LEVEL_DANGER = 31.0             # 判定“危险洪水事件”的真实阈值

# 风险计算参数
DEPTH_NORM = 2.5                # 将水深归一化到[0,1]时的特征深度
ASSET_VALUE_PER_KM2 = 1800.0    # 单位面积资产价值（万元/km2）

# 图形输出控制
SHOW_FIGURE = True
SAVE_FIGURE = False
FIG_NAME = "chapter6_flood_risk_warning.png"


def rating_curve(q):
    """水位-流量关系曲线。"""
    q = np.maximum(q, 1.0)
    return H0 + RATING_A * np.power(q, RATING_B)


def build_terrain(rng):
    """构造教学用地形DEM（高程场）。"""
    x = np.linspace(0, 1, NX)
    y = np.linspace(0, 1, NY)
    X, Y = np.meshgrid(x, y)

    # 大尺度地形趋势 + 起伏项
    base = 28.5 + 4.0 * (1 - Y) + 1.2 * np.sin(3 * np.pi * X) * np.cos(2 * np.pi * Y)
    noise = rng.normal(0, 0.8, size=(NY, NX))
    dem = ndimage.gaussian_filter(base + noise, sigma=2.2)
    return X, Y, dem


def build_river_distance():
    """构造弯曲河道并计算每个网格到河道的欧式距离。"""
    cols = np.arange(NX)
    river_row = (NY * (0.52 + 0.18 * np.sin(2 * np.pi * cols / NX))).astype(int)
    river_row = np.clip(river_row, 0, NY - 1)

    river_mask = np.zeros((NY, NX), dtype=bool)
    river_mask[river_row, cols] = True

    # 距离变换：计算每个格点到最近河道格点的距离（单位：网格）
    dist_to_river = ndimage.distance_transform_edt(~river_mask)
    return river_mask, dist_to_river


def calc_metrics(obs, pred):
    """二分类预警KPI：POD, FAR, CSI。"""
    hits = np.sum(obs & pred)
    misses = np.sum(obs & (~pred))
    false_alarms = np.sum((~obs) & pred)

    pod = hits / (hits + misses + 1e-12)
    far = false_alarms / (hits + false_alarms + 1e-12)
    csi = hits / (hits + misses + false_alarms + 1e-12)
    return pod, far, csi, hits, misses, false_alarms


def print_kpi_table(kpi):
    """打印KPI结果表格。"""
    print("\n=== KPI结果表（第6章：洪水风险图与预警系统）===")
    print(f"{'指标':<24} | {'数值':>14}")
    print("-" * 43)
    for k, v in kpi.items():
        if isinstance(v, float):
            print(f"{k:<24} | {v:>14.3f}")
        else:
            print(f"{k:<24} | {str(v):>14}")


def main():
    rng = np.random.default_rng(SEED)

    # 2) 构建地形、河道、暴露度与脆弱性
    X, Y, dem = build_terrain(rng)
    river_mask, dist_to_river = build_river_distance()

    # 暴露度：假设城镇中心在(0.62, 0.42)附近
    exposure = 0.35 + 0.65 * np.exp(-((X - 0.62) ** 2 + (Y - 0.42) ** 2) / 0.035)

    # 脆弱性：地势越低，脆弱性越高
    dem_norm = (dem - dem.min()) / (dem.max() - dem.min() + 1e-12)
    vulnerability = np.clip(0.25 + 0.75 * (1 - dem_norm), 0, 1)

    # 3) 随机生成洪峰事件（真实）与预报洪峰（带误差）
    q_true = genextreme.rvs(c=GEV_C, loc=GEV_LOC, scale=GEV_SCALE, size=N_EVENTS, random_state=rng)
    q_true = np.clip(q_true, 200.0, None)

    q_forecast = q_true + rng.normal(0, FORECAST_Q_STD, size=N_EVENTS)
    q_forecast = np.clip(q_forecast, 50.0, None)

    wl_true = rating_curve(q_true)
    wl_forecast = rating_curve(q_forecast)

    # 4) 洪水淹没深度与风险图计算
    effective_wl = wl_true[:, None, None] - DIST_ATTENUATION * dist_to_river[None, :, :]
    depth_all = np.maximum(0.0, effective_wl - dem[None, :, :])

    hazard = np.clip(depth_all / DEPTH_NORM, 0, 1)
    risk_all = hazard * exposure[None, :, :] * vulnerability[None, :, :]
    mean_risk_map = risk_all.mean(axis=0)

    flooded_area = (depth_all > 0.05).sum(axis=(1, 2)) * CELL_AREA_KM2
    loss_wanyuan = risk_all.sum(axis=(1, 2)) * ASSET_VALUE_PER_KM2 * CELL_AREA_KM2

    # 5) 预警分级（按预报水位）
    warning_level = np.full(N_EVENTS, "蓝色", dtype=object)
    warning_level[wl_forecast >= LEVEL_YELLOW] = "黄色"
    warning_level[wl_forecast >= LEVEL_ORANGE] = "橙色"
    warning_level[wl_forecast >= LEVEL_RED] = "红色"

    danger_obs = wl_true >= LEVEL_DANGER
    warning_pred = wl_forecast >= LEVEL_ORANGE  # 将橙色及以上视为“触发警报”

    pod, far, csi, hits, misses, false_alarms = calc_metrics(danger_obs, warning_pred)
    rmse = np.sqrt(np.mean((wl_forecast - wl_true) ** 2))

    # 6) 打印KPI表格
    kpi = {
        "总模拟场次": N_EVENTS,
        "危险洪水场次": int(np.sum(danger_obs)),
        "水位预报RMSE(m)": rmse,
        "命中率POD": pod,
        "空报率FAR": far,
        "临界成功指数CSI": csi,
        "平均淹没面积(km2)": float(np.mean(flooded_area)),
        "95%分位淹没面积(km2)": float(np.percentile(flooded_area, 95)),
        "平均风险指数": float(np.mean(mean_risk_map)),
        "平均预期损失(万元)": float(np.mean(loss_wanyuan)),
        "命中数Hits": int(hits),
        "漏报数Misses": int(misses),
        "空报数FalseAlarms": int(false_alarms),
    }
    print_kpi_table(kpi)

    print("\n预警等级统计：")
    for c in ["蓝色", "黄色", "橙色", "红色"]:
        print(f"{c}: {np.sum(warning_level == c)} 场")

    # 7) 选取95%分位洪峰对应事件展示淹没图
    rep_idx = np.argsort(q_true)[int(0.95 * N_EVENTS) - 1]
    rep_depth = depth_all[rep_idx]
    rep_peak = q_true[rep_idx]

    # 构造代表性过程线（用于展示阈值预警逻辑）
    t = np.linspace(0, 72, 320)  # 小时
    g = gamma.pdf(t, a=5.0, scale=6.0)
    g = g / (g.max() + 1e-12)
    q_hydro = 300 + rep_peak * g
    wl_hydro = rating_curve(q_hydro)

    # 8) 绘图
    plt.rcParams["axes.unicode_minus"] = False
    plt.rcParams["font.sans-serif"] = ["SimHei", "Microsoft YaHei", "Arial Unicode MS", "sans-serif"]

    fig, axes = plt.subplots(2, 2, figsize=(13, 9))

    # 图1：地形与河道
    im0 = axes[0, 0].imshow(dem, cmap="terrain")
    axes[0, 0].contour(river_mask.astype(float), levels=[0.5], colors="deepskyblue", linewidths=1.2)
    axes[0, 0].set_title("地形DEM与河道")
    axes[0, 0].set_xlabel("X网格")
    axes[0, 0].set_ylabel("Y网格")
    fig.colorbar(im0, ax=axes[0, 0], shrink=0.85, label="高程(m)")

    # 图2：代表事件淹没深度图
    im1 = axes[0, 1].imshow(rep_depth, cmap="Blues")
    axes[0, 1].set_title("95%分位洪峰事件淹没深度")
    axes[0, 1].set_xlabel("X网格")
    axes[0, 1].set_ylabel("Y网格")
    fig.colorbar(im1, ax=axes[0, 1], shrink=0.85, label="水深(m)")

    # 图3：平均风险图
    im2 = axes[1, 0].imshow(mean_risk_map, cmap="YlOrRd", vmin=0, vmax=1)
    axes[1, 0].set_title("平均洪水风险图（Hazard×Exposure×Vulnerability）")
    axes[1, 0].set_xlabel("X网格")
    axes[1, 0].set_ylabel("Y网格")
    fig.colorbar(im2, ax=axes[1, 0], shrink=0.85, label="风险指数(0-1)")

    # 图4：代表性水位过程与预警阈值
    axes[1, 1].plot(t, wl_hydro, color="navy", lw=2, label="代表事件水位过程")
    axes[1, 1].axhline(LEVEL_YELLOW, color="gold", ls="--", lw=1.5, label="黄色阈值")
    axes[1, 1].axhline(LEVEL_ORANGE, color="darkorange", ls="--", lw=1.5, label="橙色阈值")
    axes[1, 1].axhline(LEVEL_RED, color="red", ls="--", lw=1.5, label="红色阈值")
    axes[1, 1].set_title("预警阈值示意（代表洪水过程）")
    axes[1, 1].set_xlabel("时间(h)")
    axes[1, 1].set_ylabel("水位(m)")
    axes[1, 1].grid(alpha=0.25)
    axes[1, 1].legend(fontsize=9)

    plt.tight_layout()

    if SAVE_FIGURE:
        plt.savefig(FIG_NAME, dpi=200, bbox_inches="tight")
        print(f"\n图件已保存：{FIG_NAME}")

    if SHOW_FIGURE:
        plt.show()


if __name__ == "__main__":
    main()
```

代码解读（约800字）  
这段脚本对应第6章“洪水风险图与预警系统”的基础框架，核心思路是把“洪水致灾强度、承灾体暴露、系统脆弱性”三部分耦合起来，再用预报结果驱动分级预警并用KPI评价效果。代码先在参数区集中定义网格规模、事件数量、阈值水位、预报误差、风险参数等关键量，这样教师在课堂演示时可直接改变量观察结果变化，便于讲清模型敏感性。随后`build_terrain`构造了一个教学型DEM：既有整体坡降，也有局部起伏，再通过高斯平滑消除离散噪声，让地形更接近实际流域的连续高程特征。`build_river_distance`定义弯曲河道并计算每个网格到河道的距离，这是后续“离河越远、洪水位越低”的空间衰减基础。  
在水文随机性方面，脚本使用GEV分布抽样洪峰流量，体现极端洪水的统计特征；再叠加高斯预报误差生成“预报洪峰”，对应业务中不可避免的预见期误差。通过`rating_curve`把流量转换为水位，形成风险图与预警判别的统一物理量。淹没深度计算采用`effective_wl - dem`：其中`effective_wl`等于河道水位减去距离衰减项，保证了洪水从主河道向外扩散时能量递减的基本机理。深度归一化后得到`hazard`，再与`exposure`和`vulnerability`逐格相乘形成`risk_all`，这正是6.1里“风险=危险性×暴露度×脆弱性”的可计算表达。  
预警系统部分，脚本按预报水位映射蓝黄橙红四级，且把“橙色及以上”视作触发警报，用于和真实危险事件做二分类对照。KPI采用POD、FAR、CSI三项：POD高说明漏报少，FAR低说明空报少，CSI综合反映命中质量；再配合水位RMSE、平均淹没面积、95%分位淹没面积、平均风险指数和平均预期损失，形成“预报-风险-决策”一体化评估。输出表格便于直接放入教材实验结果部分。  
可视化方面，四幅图分别展示“地形与河道”“代表事件淹没深度”“平均风险分布”“水位过程与阈值线”，实现从机理到决策的闭环表达。尤其最后一图把连续水位过程与分级阈值叠加，能直观看到何时跨越黄色、橙色、红色阈值，便于讲解预警启动时机。整体上，这份脚本不是追求高精度实务预报，而是强调理论框架清晰、参数可调、流程完整，适合作为第6章课堂仿真与作业模板。  

当前会话命令执行被策略拦截，我无法在此环境直接实跑；脚本结构和语法按标准`numpy/scipy/matplotlib`写法组织，可直接在本地Python环境运行。


## 参考文献

1. Beven, K. J., & Kirkby, M. J. (1979). A physically based, variable contributing area model of basin hydrology. *Hydrological Sciences Bulletin*, 24(1), 43-69.
2. Krzysztofowicz, R. (2001). The case for probabilistic forecasting in hydrology. *Journal of Hydrology*, 249(1-4), 2-9.
3. Cloke, H. L., & Pappenberger, F. (2009). Ensemble flood forecasting: A review. *Journal of Hydrology*, 375(3-4), 613-626.
4. Lei et al. (2025a). 水系统控制论：基本原理与理论框架. *南水北调与水利科技(中英文)*. DOI: 10.13476/j.cnki.nsbdqk.2025.0077
5. Nash, J. E., & Sutcliffe, J. V. (1970). River flow forecasting through conceptual models part I—A discussion of principles. *Journal of Hydrology*, 10(3), 282-290.

---

## 本章小结

本章围绕洪水风险图编制与分级预警系统构建，系统阐述了从风险量化理论到预警性能评估的完整技术链条。

1. **洪水风险三要素框架**：洪水风险的量化遵循"致灾因子-暴露度-脆弱性"三元框架，综合表达为 $\text{Risk} = \text{Hazard} \times \text{Exposure} \times \text{Vulnerability}$。其中，致灾因子（Hazard）反映洪水淹没深度和发生频率，暴露度（Exposure）刻画承灾体的空间密度，脆弱性（Vulnerability）描述承灾体在特定致灾强度下的损失比例。三者的网格化乘积运算是编制定量风险图的核心计算步骤，为土地利用规划和防灾减灾资源配置提供空间决策依据。

2. **极值洪水的统计建模**：洪峰流量的频率分析通常采用广义极值分布（GEV），其累积分布函数形式为 $F(x;\mu,\sigma,\xi) = \exp\{-[1+\xi(x-\mu)/\sigma]^{-1/\xi}\}$，三个参数分别控制位置、尺度和形状特征。当形状参数 $\xi < 0$ 时（Weibull型），分布存在有限上界，适用于降雨控制型流域；当 $\xi > 0$ 时（Fréchet型），分布具有重尾特征，常见于冰雪融水驱动型洪水。通过 Monte Carlo 方法从拟合的 GEV 分布抽样大量洪水场次，可系统评估预警系统在不同量级洪水下的响应特性。

3. **分级预警阈值与水位-流量关系**：将流量转换为可直接与防洪标志水位对照的水位值，是连接水文预报与预警触发的关键一步。水位-流量关系 $H = H_0 + A \cdot Q^B$ 基于Manning公式推导并由现场率定确定参数，体现了断面几何形态和糙率的综合效应。蓝黄橙红四级阈值的划定应与当地防洪标准（如 5 年、20 年、50 年一遇）对应，并在实际业务中需定期根据河床冲淤变化重新率定。

4. **预警性能评估指标体系**：预警系统的效果量化采用二分类混淆矩阵框架，核心指标包括探测概率 $\text{POD} = \text{TP}/(\text{TP}+\text{FN})$、误报率 $\text{FAR} = \text{FP}/(\text{TP}+\text{FP})$ 和临界成功指数 $\text{CSI} = \text{TP}/(\text{TP}+\text{FP}+\text{FN})$。POD 与 FAR 之间存在内在权衡关系：降低阈值可提高 POD 但同时增大 FAR，CSI 提供了综合衡量命中质量的单一指标。在防洪减灾背景下，漏报（低 POD）的社会代价通常远高于空报（高 FAR），阈值设计应体现这一非对称性。

5. **预报误差传播与预警不确定性**：实际业务中洪峰流量预报存在不可消除的随机误差，叠加于真实洪峰之上的高斯误差 $\varepsilon \sim \mathcal{N}(0, \sigma_Q^2)$ 会导致预警触发时机的不确定性。当预报流量接近预警阈值时，误差的影响最为显著，此时可引入概率预警机制，以"超越某阈值的概率超过给定置信水平"替代确定性阈值判别，从而更科学地处理预报不确定性对决策的影响。

---

## 思考与练习

**1.** 请解释洪水风险三要素框架中"暴露度"和"脆弱性"的本质区别，并举例说明在城市洪涝情景中，如何从土地利用数据和建筑物类型数据分别量化这两个要素。此外，分析将三要素简单相乘作为风险综合指数的假设前提及其局限性。

**2.** 广义极值分布（GEV）包含 Gumbel（$\xi=0$）、Fréchet（$\xi>0$）和 Weibull（$\xi<0$）三种子类型。请从极值理论出发，推导 Gumbel 分布的概率密度函数，并说明形状参数 $\xi$ 的物理意义。给定一组年最大洪峰流量序列（至少20年），说明如何利用最大似然估计法拟合 GEV 参数，以及如何用重现期表达法计算百年一遇设计洪峰。

**3.** 某流域防洪预警系统采用三级水位阈值（黄色28.5 m、橙色30.0 m、红色31.5 m），但实地调研发现当地居民对橙色预警的响应率仅为40%，而对红色预警响应率达90%。请从风险沟通与行为决策视角，分析影响公众预警响应率的主要因素，并提出至少三项可量化评估效果的预警传播改进措施。

**4.** 基于本章 Monte Carlo 仿真框架，设计一个研究方案，评估预报提前时间（Lead Time）从1小时增加至6小时对 CSI 的影响规律。需明确：①如何建模预报误差随提前时间增大而增大的关系；②如何定义"有效撤离时间窗口"并将其纳入评估框架；③预期结果的定性趋势及其对实际应急管理的启示。

**5.** 目前洪水风险图编制主要基于历史水文数据和当前地形条件，未考虑气候变化引起的洪水频率曲线非平稳性。请综述非平稳极值分析（Non-stationary Extreme Value Analysis）的主要建模方法，分析时变GEV参数（如 $\mu(t) = \mu_0 + \mu_1 t$）的拟合方法与统计检验策略，并讨论非平稳假设对百年一遇设计洪峰估计结果的影响。


6. HOCHREITER S, SCHMIDHUBER J. Long short-term memory[J]. Neural Computation, 1997, 9(8): 1735-1780. DOI: 10.1162/neco.1997.9.8.1735.
7. 中华人民共和国国家质量监督检验检疫总局, 中国国家标准化管理委员会. 水文情报预报规范: GB/T 22482-2008[S]. 北京: 中国标准出版社, 2009.
8. 中华人民共和国水利部. 水文情报预报规范: SL 250-2000[S]. 北京: 中国水利水电出版社, 2000.
