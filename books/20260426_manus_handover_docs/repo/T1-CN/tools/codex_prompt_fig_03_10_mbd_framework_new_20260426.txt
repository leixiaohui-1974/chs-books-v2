请在当前仓库中只做一件事：从零生成一张新的中文教材插图，并保存为 `T1-CN/H/fig_03_10_mbd_framework_codex_20260426.png`。

这是新文件，不要修改、转换、修复、覆盖任何现有图片。

图题：`图3-2 MBD四层一闭环架构`

统一风格要求：
学术教材风格，白色背景，蓝色主调（默认可用 #1565C0），扁平矢量感，中文标注为主，横向 4:3 构图，适合书稿内嵌排版。

画面内容要求：
绘制一张四层架构图，并带有完整闭环反馈。自上而下依次为：第一层“ODD定义”，第二层“模型决策层”，其中内部再细分四类模型模块；第三层“在环验证层”，清楚标出“MIL→SIL→HIL”的渐进链路；第四层“现场执行层”。在右侧或底部增加一条虚线反馈通道，将现场数据回传到上面各层，形成完整闭环。主链路箭头方向要明确，反馈链路与主链路应在视觉上区分。图中应强调“定义—建模—验证—部署—反馈”的工程闭环逻辑。

执行限制：
1. 必须直接生成 PNG 图片文件；
2. 不要只输出文档；
3. 不要读取或复用已有图片作为输入；
4. 不要画成海报风、宣传图风、UI 截图风或写实营销图；
5. 不要大段中英双语混排，但 MIL、SIL、HIL、ODD 可保留英文缩写；
6. 如适合，请优先采用程序化绘制方式，确保中文文字、框图、箭头和反馈闭环结构清晰可读；
7. 完成后只需简短报告输出路径与采用的方法。
