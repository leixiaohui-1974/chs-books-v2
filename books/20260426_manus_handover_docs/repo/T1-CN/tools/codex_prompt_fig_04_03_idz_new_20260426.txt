请在当前仓库中只做一件事：从零生成一张新的中文教材插图，并保存为 `T1-CN/H/fig_04_03_idz_codex_20260426.png`。

这是新文件，不要修改、转换、修复、覆盖任何现有图片。

图题：`图4-2 单渠池 IDZ 模型示意图`

统一风格要求：
学术教材风格，白色背景，蓝色主调，扁平矢量感，中文标注为主，横向 4:3 构图，适合书稿内嵌排版。

画面内容要求：
绘制一个单渠池纵剖面教材图。左侧为上游闸门与输入控制，右侧为下游测点与水位输出，中间为单渠池渠道断面。必须清晰标注以下对象与符号：
- 上游闸门输入 `U(s)`
- 渠池长度 `L`
- 传输延迟 `τ_d`
- 末端下游水位输出 `Y(s)`
- 渠道水面线与波传播方向

在图的上方或右上角，要清楚写出传递函数：
`G(s) = (1 + τ_m·s) · exp(-τ_d·s) / (A_s·s)`

同时通过小标签说明三个参数的角色：
- `A_s`：等效蓄水面积
- `τ_d`：纯延迟 / 传播时滞
- `τ_m`：最小相位或初始动态修正项

目标是让读者一眼看明白：IDZ 模型把连续水力过程压缩为“输入闸门—延迟传播—末端水位响应”的低维传递关系，适合控制器设计与实时计算。

执行限制：
1. 必须直接生成 PNG 图片文件；
2. 不要只输出文档；
3. 不要读取或复用已有图片作为输入；
4. 不要画成写实工程照片或宣传海报；
5. 请优先采用程序化绘制方式，保证公式、箭头、参数标签和纵剖面结构清晰；
6. 完成后只需简短报告输出路径与采用的方法。
