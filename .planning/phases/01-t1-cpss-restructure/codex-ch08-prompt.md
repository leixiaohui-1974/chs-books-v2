# Task: 重构 T1-CN/ch08_final.md — CPSS统一框架

## 概念背景

请先阅读 `.planning/phases/01-t1-cpss-restructure/CONTEXT.md`，这是你执行本任务所需的全部概念背景。

## 当前问题

ch08当前标题是"CPSS框架：信息-物理-社会融合控制"，但内容聚焦在**水电站CPSS**（水力-机械-电气耦合），非常窄。需要扩展为CPSS作为**水系统控制论的统一框架**。

## 具体修改要求

### 修改1: 扩展§8.1引言

在§8.1.3（CPSS框架在CHS体系中的定位）中，在现有"从CHS到CPSS"段落之后，增加一段论述：

CPSS不仅适用于水电站的水-机-电耦合，更是描述所有水系统运行的统一框架。任何水系统——无论是单站水电站、梯级枢纽、长距离调水干线还是城市供水管网——其运行都涉及Physical空间（水物理过程）、Cyber空间（模型、算法、控制器）和Social空间（调度规程、法规、社会需求）的耦合。后续几节将从统一视角展开这三个空间的交互机制。

约200字，学术风格。

### 修改2: 新增一节"CPSS三空间与三重反馈回路"

在§8.1之后（现有§8.2之前），插入新节。内容要点：

1. 三重反馈回路（这是核心创新点）：
   - 回路1 Physical↔Cyber（秒~分钟级）：传感器→控制器→执行器→物理过程→传感器
   - 回路2 Cyber↔Social（小时~天级）：优化方案→调度员审批→执行→效果评估→方案更新
   - 回路3 Social↔Physical（月~年级）：社会需求变化→标准修编→工程改造→物理系统更新

2. 关键论述：水系统复杂性的真正来源不是物理过程本身（Saint-Venant方程已有200年历史），而是三个空间的耦合反馈。传统水利工程只关注回路1（自动控制），忽视了回路2（人机协同）和回路3（社会演化）。CHS通过CPSS框架将三者纳入统一视野。

3. 画一个文本架构图（用markdown代码块）展示三重反馈。

约800字。

### 修改3: 新增一节"确定性工作流与灵活工作流"

紧接上一节之后插入。内容要点：

1. Physical空间 → 确定性工作流（物理因果律不可违背，操作时序严格）
2. Social空间 → 灵活工作流（人类决策情境依赖，可中断可改序）
3. Cyber空间 → 混合（算法流程确定，但策略可自适应）
4. 与AI工作流概念的桥接：DAG Workflow=确定性，Dynamic Routing=灵活，Agent Loop=闭环

约500字。

### 修改4: 新增一节"闭环控制与开环执行的范式区分"

内容要点：

1. 时间步内闭环（控制）：每个时间步内多模型耦合迭代
2. 开环串行（仿真）：模型A跑完全时段再传给模型B
3. 用文本图对比两种范式
4. 当前水利行业主流做法是开环串行，这不是控制而是仿真

约500字。

### 修改5: 新增一节"基于CHS的四预闭环体系"

内容要点：

1. 水利部四预（预报预警预演预案）的现状：开环串行流水线
2. CHS视角下的四预闭环重构：
   - 预报 = Sensing + Digital Twin 前向推演
   - 预警 = ODD状态评估
   - 预演 = XIL验证（MIL/SIL/HIL/OIL对应四层）
   - 预案 = Control的实时决策输出（不是静态文档）
3. 宏观尺度（天~周，灵活工作流，Social主导）vs 实时尺度（分钟~小时，确定性工作流，Physical主导）
4. 多模态运行(G0正常~G4极端)作为工作流模式切换的论述

约800字。

## 重要约束

- **保留现有水电站CPSS内容**，不要删除。新增内容插入在现有内容之前或之后
- 不点名具体工程，用"某长距离调水干线"等通用描述
- 风格与原文一致：学术严谨+工程直觉
- 文献引用格式 [8-xx]，新增引用编号接续现有最大编号
- 公式用LaTeX格式
- 总新增约2800字
