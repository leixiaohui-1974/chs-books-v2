Now I have all the data needed. Here is the consolidated repair plan:

---

# 第03章修复方案

## 共识问题（P0，必须修复）

### P0-1 图片路径硬编码为GitHub URL（三方共识：Claude+Codex+Gemini）
**问题**：图3-1(L96)和图3-2(L276)使用 `https://raw.githubusercontent.com/...` 远程URL，不符合全书 `./H/` 本地路径规范。本地文件已确认存在。
**修复操作**：
- L96: 替换为 `![八原理依赖导图](./H/fig_03_01_principles_dag.png)`
- L276: 替换为 `![图3-2：MBD四层一闭环架构](./H/fig_03_10_mbd_framework.png)`

### P0-2 图3-2 Markdown混入HTML残片（三方共识：Claude+Codex+Gemini）
**问题**：L276 `![图3-2：MBD四层一闭环架构</b></div>](url)` 混入 `</b></div>` 残片，会导致排版编译失败。
**修复操作**：L276 替换为 `![图3-2：MBD四层一闭环架构](./H/fig_03_10_mbd_framework.png)`（与P0-1合并修复）

### P0-3 僵尸引用 [3-12]~[3-15]（三方共识：Claude+Codex+Gemini）
**问题**：4条参考文献在正文中从未被引用。
**修复操作**：在正文中补充引用落点——
- [3-12] Skogestad → L125 §3.2 "定量可靠的响应映射"后加引用
- [3-13] Blanke → L177 §3.5 "安全包络不会降低系统性能"后加引用
- [3-14] DO-178C → L185 §3.6 "MIL→SIL→HIL"后与ISO 21448并列引用
- [3-15] 雷晓辉2016 → L155 §3.4 "大型水利系统天然具有空间分布广"后加引用

### P0-4 跨章引用错误：§1.6应为§1.4（Claude+Codex均标记）
**问题**：L258 写"第一章§1.6"，但ch01_final.md中设计锁定效应位于**§1.4**（已经Grep验证确认：L344 `## 1.4 设计锁定效应`）。
**修复操作**：L258 `§1.6` → `§1.4`

### P0-5 MBD四层名称前后不一致（Codex P0）
**问题**：L279图注写"ODD定义层→模型决策层→在环验证层→现场执行层"，L293正文写"第四层：闭环迭代层"，二者矛盾。同时L291验证链写"SIM→SIL→HIL"与全书统一的"MIL→SIL→HIL"不一致。
**修复操作**：
- L291: `SIM（仿真）→ SIL` → `MIL（模型在环）→ SIL`
- L279图注改为与正文四层对齐："ODD定义层→模型与决策层→在环验证层→闭环迭代层（数据回馈）"
- 或将L293 "闭环迭代层"改为"现场执行层+数据回馈闭环"——建议采用前者，图注对齐正文

---

## 重要问题（P1，建议修复）

### P1-1 引导案例第三断裂面原理映射存疑（Claude P1-1）
**问题**：L20 "缺乏统一框架→P5+P7"，逻辑牵强。核心问题更应映射到P1（统一建模语言）和P3（层级优先级规则）。
**修复操作**：L20 修改为 `→ P5（在环验证）在联合工况下暴露整合缺陷 + P1/P3（传递函数化/分层分布式）提供跨子系统的统一语言和优先级规则`，并加注"注意同一原理可同时回应多个断裂面"。

### P1-2 术语表"全生命周期自主演进"首次出现位置错误（Claude P1-2）
**问题**：L447 术语表标注首次出现于§3.10，但实际首次出现在L233 **§3.9**。
**修复操作**：L447 `§3.10` → `§3.9`

### P1-3 术语表"灰度发布"与正文"灰度验证"不一致（Claude P1-3）
**问题**：术语表L449写"灰度发布"且标注§3.10，正文L239/L246使用的是"灰度验证"。
**修复操作**：L449 术语改为"灰度验证"，首次出现改为"§3.9"

### P1-4 概念速览框核心概念数量标注不准（Claude P1-4）
**问题**：L32 标注"5-8个"，实际列出9个。
**修复操作**：L32 `（5-8 个）` → `（9 个）`

### P1-5 本章目标中"八原理与WNAL等级（§7）"引用错误（Codex P1）
**问题**：L49 写"八原理与WNAL等级（§7）"，但正式映射在第十章不在第七章。
**修复操作**：L49 `（§7）` → `（§10）`

### P1-6 L3与P6/P7准入口径跨章不一致（Codex P0级，但涉及跨章协调降为P1）
**问题**：L365本章写L3只需P1-P5，P6/P7为"强烈推荐"；但ch07写"认知能力是L3+必要条件之一"，ch10映射表将P6/P7在L3标"初步"。
**修复操作**：本章L365措辞保持不变（作为概览定性表述合理），但在末尾加注 `（注：第七章§7.7将进一步讨论P6在L3级的条件性要求，详见§10.7统一口径表）`。同时需在ch07和ch10中做对应协调（列入跨章修复清单）。

### P1-7 黄区自动拉回表述过满（Codex P1）
**问题**：L173 "在不需要人工干预的情况下把状态拉回绿区"过于理想化。
**修复操作**：L173 改为"在不需要人工干预的情况下**尝试**把状态拉回绿区；**若预计恢复时间超过黄区到红区的剩余裕度，则立即请求人工介入或进入最小风险条件（MRC）**"

### P1-8 "设计模型可直接用于运行数字孪生"表述过度（Codex P1）
**问题**：L316 "可以直接用于运行阶段的数字孪生，避免重复建模"工程上过于乐观。
**修复操作**：L316 改为"可以**作为基础资产**用于运行阶段的数字孪生，**但不得省略运行期的再辨识、I/O映射和边界条件重估**"

### P1-9 推荐阅读未标注参考文献编号（Claude P1-6）
**修复操作**：推荐阅读各条目末尾补注对应编号，如"[3-10]"、"[3-2]"、"[3-5]"等。

---

## 建议改进（P2，可选）

1. **§3.2增加最小公式锚点**（Claude P2-3 + Codex P1）：在§3.2补一个Family α传递函数示例 `$G(s)=(1+\tau_m s)e^{-\tau_d s}/(A_s \cdot s)$`，为第四章做铺垫。
2. **§3.10.3水锤类比可更精确**（Claude P2-2）：将"MBD的雏形"改为"MBD中在环验证思想的工程雏形"。
3. **§3.10.2 ODD解释过于抽象**（Gemini P1-1）：补充一句通俗解释："简而言之，ODD就是为自动控制系统画一个'安全活动圈'"。
4. **§3.3 补充"状态"概念的直觉映射**（Gemini P1-2）：点明"状态≠仪表读数，而是决定系统未来走向的最小内部变量集合"。
5. **§3.8 HitL/HotL/HootL大小写统一**（Gemini P2-2）：建议统一为全大写HITL/HOTL/HOOTL。
6. **§3.4 四层架构与P1a Layer 0-3的对应注释**（Claude P2-4）。
7. **§3.1.5 P1-P5中P5/P6/P8各自对应的控制本质可展开**（Claude P2-1）。
8. **练习题L3第5题增加具体约束条件**（Claude P2-5）。
9. **引导案例补充脱敏工程特征数据**（Gemini P2-3）。

---

## 统计

- **自引率**: 3/15 = **20%**（[3-10]雷2025a + [3-11]雷2025d + [3-15]雷2016）。修复僵尸引用后若补入[3-15]则仍为3/15=20%，在目标区间15-25%内，**合格**。
- **图表路径检查**: **0/2 正确**（2张图均使用远程URL，本地文件 `H/fig_03_01_principles_dag.png` 和 `H/fig_03_10_mbd_framework.png` 均已确认存在）
- **交叉引用检查**:
  - L258 `§1.6` → **错误**，应为 `§1.4`（已确认ch01 L344）
  - L49 `（§7）` → **错误**，映射表在第十章
  - L365 `§10.7` → 正确
  - L194 `§10.5.1` → 待验证
  - L101 `§10.3.3` → 待验证

---

## 修复执行摘要

| 优先级 | 数量 | 预计改动行数 |
|--------|------|-------------|
| P0 | 5项 | ~15行 |
| P1 | 9项 | ~20行 |
| P2 | 9项 | ~15行（可选） |

**建议执行顺序**：P0全部 → P1-1~P1-5（本章内可独立完成）→ P1-6（需跨章协调）→ P1-7~P1-9 → P2酌情选取。

需要我现在开始执行P0修复吗？
