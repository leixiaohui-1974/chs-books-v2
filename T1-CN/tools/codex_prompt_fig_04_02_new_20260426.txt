请在当前仓库中只做一件事：从零生成一张新的中文教材插图，并保存为 `T1-CN/H/fig_04_02_reservoir_codex_20260426.png`。

这是新文件，不要修改、转换、修复、覆盖任何现有图片。

图题：`图4-1 水库状态空间模型示意图`

统一风格要求：
学术教材风格，白色背景，蓝色主调，扁平矢量感，中文标注为主，横向 4:3 构图，适合书稿内嵌排版。

画面内容要求：
绘制一个“水库横截面剖面图 + 状态空间变量标注”组合教材图。左侧主体为水库剖面：上游来水进入库区，下游通过闸门或出流建筑物出库。必须清楚标注以下物理量：
- 入库流量 `Q_in`
- 出库流量 `Q_out`
- 库容 `V`
- 水位 `h`
- 闸门开度 `g`

在右侧或下方，用简洁表格或框图形式标注四元组 `(x, u, d, y)` 的对应关系，例如：
- `x`：库容 / 水位状态
- `u`：闸门开度
- `d`：入库扰动或来水过程
- `y`：水位 / 出流观测量

目标是让读者一眼看明白：水库并不只是几何水体，而是可以被抽象成状态、控制、扰动、输出四类变量共同构成的状态空间系统。

执行限制：
1. 必须直接生成 PNG 图片文件；
2. 不要只输出文档；
3. 不要读取或复用已有图片作为输入；
4. 不要画成写实风景图或宣传海报；
5. 请优先采用程序化绘制方式，保证剖面、箭头、变量和四元组关系清晰；
6. 完成后只需简短报告输出路径与采用的方法。
