请在当前仓库中只做一件事：从零生成一张新的中文教材插图，并保存为 `T1-CN/H/fig_02_03_codex_20260426.png`。

这是新文件，不要修改、转换、修复、覆盖任何现有图片。

图题：`图2-3 多时间尺度分层控制链`

统一风格要求：
学术教材风格，白色背景，蓝色主调，扁平矢量感，中文标注为主，横向 4:3 构图，适合书稿内嵌排版。

画面内容要求：
绘制一个四层水平带状结构图，清晰表现多时间尺度分层控制链。四层依次为：规划层（日 / 周，浅蓝）、协调层（小时，蓝）、调节层（分钟，深蓝）、执行层（秒，藏蓝）。每层都要有清晰的中文标签和时间尺度标签。

层间关系必须同时包含两类信息流：
1. 自上而下的“目标 / 约束”下发箭头；
2. 自下而上的“状态报告 / 执行反馈”上行箭头。

右侧可以用窄栏补充每层典型方法，例如：
- 规划层：长期优化 / 配水计划
- 协调层：区域协调 / 调度分解
- 调节层：MPC / 滚动优化
- 执行层：PLC / PID / 现场控制

目标是让读者一眼看明白：越上层时间尺度越长、抽象程度越高；越下层响应越快、越贴近设备执行；整条链路通过上下行信息流形成闭环协同。

执行限制：
1. 必须直接生成 PNG 图片文件；
2. 不要只输出文档；
3. 不要读取或复用已有图片作为输入；
4. 不要画成海报风、宣传图风、UI 截图风或写实营销图；
5. 请优先采用程序化绘制方式，确保层级、箭头、标签和时间尺度清晰；
6. 完成后只需简短报告输出路径与采用的方法。
