请在当前仓库中只做一件事：从零生成一张新的中文教材插图，并保存为 `T1-CN/H/fig_02_01_codex_20260426.png`。

这是新文件，不要修改、转换、修复、覆盖任何现有图片。

图题：`图2-1 水系统状态—输入—输出—扰动框图`

统一风格要求：
学术教材风格，白色背景，蓝色主调（默认可用 #1565C0），扁平矢量感，中文标注为主，横向 4:3 构图，适合书稿内嵌排版。

画面内容要求：
绘制一张经典控制框图。中央主体为“水利系统 f(·)”或“水系统动力学模型”方框。左侧为输入 `u_k`，标注示例“闸门 / 泵站”；上方为扰动 `d_k`，标注示例“降雨 / 取水”；右侧为输出 `y_k`，标注示例“水位 / 流量”；底部为慢变参数 `θ_k`。输出端叠加测量噪声 `v_k`。输出通过反馈回路返回左侧控制器，再由控制器作用到系统输入。需要清楚体现“输入—系统—输出—反馈”的控制逻辑，同时让扰动、慢变参数、噪声三类外生因素与主闭环区分明显。

执行限制：
1. 必须直接生成 PNG 图片文件；
2. 不要只输出文档；
3. 不要读取或复用已有图片作为输入；
4. 不要画成海报风、宣传图风、UI 截图风或写实营销图；
5. 不要大段中英双语混排，但 `u_k`、`d_k`、`y_k`、`θ_k`、`v_k` 可保留数学符号；
6. 如适合，请优先采用程序化绘制方式，确保框图、箭头、反馈回路和中文文字清晰可读；
7. 完成后只需简短报告输出路径与采用的方法。
