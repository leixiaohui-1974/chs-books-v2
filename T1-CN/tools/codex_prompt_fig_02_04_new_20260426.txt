请在当前仓库中只做一件事：从零生成一张新的中文教材插图，并保存为 `T1-CN/H/fig_02_04_codex_20260426.png`。

这是新文件，不要修改、转换、修复、覆盖任何现有图片。

图题：`图2-4 异常工况四态机状态迁移图`

统一风格要求：
学术教材风格，白色背景，蓝色主调，允许状态风险色作为辅助色，扁平矢量感，中文标注为主，横向 4:3 构图，适合书稿内嵌排版。

画面内容要求：
绘制一个清晰的四态机状态迁移图。四个主要状态分别为：
- 正常态（绿色）
- 受限态（黄色）
- 降级态（橙色）
- 接管态（红色）

状态之间要有清晰箭头，标注触发条件与恢复条件。建议主迁移方向体现“风险升级”过程：正常态 → 受限态 → 降级态 → 接管态。同时保留从后续状态回到前序状态的恢复虚线箭头。

每个状态框内要写出简洁中文说明，例如：
- 正常态：自动控制 / 全性能运行
- 受限态：约束收紧 / 负荷受限
- 降级态：部分功能关闭 / 安全优先
- 接管态：人工接管 / 紧急处置

目标是让读者一眼看清楚：在异常工况下，系统不是只有“开/关”两种状态，而是通过分级降级与恢复机制实现可控过渡。

执行限制：
1. 必须直接生成 PNG 图片文件；
2. 不要只输出文档；
3. 不要读取或复用已有图片作为输入；
4. 不要画成宣传海报、UI 截图或写实场景；
5. 请优先采用程序化绘制方式，确保状态框、颜色、箭头、虚实线与标签清晰；
6. 完成后只需简短报告输出路径与采用的方法。
