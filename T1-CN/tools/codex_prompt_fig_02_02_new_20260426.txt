请在当前仓库中只做一件事：从零生成一张新的中文教材插图，并保存为 `T1-CN/H/fig_02_02_codex_20260426.png`。

这是新文件，不要修改、转换、修复、覆盖任何现有图片。

图题：`图2-2 可控模型族的三层体系`

统一风格要求：
学术教材风格，白色背景，蓝色主调，扁平矢量感，中文标注为主，横向 4:3 构图，适合书稿内嵌排版。

画面内容要求：
绘制一个三层递进金字塔或三层阶梯结构图，表现“可控模型族”的层次体系。底层是“高保真 PDE 模型”，标注维度约 `10^3`；中层是“降阶传递函数（IDZ）”，标注维度约 `10^1`；顶层是“数据增强模型”。

层间关系必须清楚：
1. 从底层到中层有“降阶方法”箭头；
2. 从顶层到中层有“数据同化”或“数据增强补偿”箭头；
3. 中层作为控制设计最适用的平衡层，应在视觉上略突出。

目标是让读者一眼看明白：模型越往下越高保真、越复杂；越往上越灵活、越依赖数据；中层是工程控制中兼顾可解释性与实时性的关键层。

执行限制：
1. 必须直接生成 PNG 图片文件；
2. 不要只输出文档；
3. 不要读取或复用已有图片作为输入；
4. 不要画成海报风、宣传图风、UI 截图风或写实营销图；
5. 数学量级可保留为 `10^3`、`10^1`，其余标注用中文；
6. 如适合，请优先采用程序化绘制方式，确保层次结构、箭头关系和中文标签清晰；
7. 完成后只需简短报告输出路径与采用的方法。
